<nav class="navbar navbar-expand-lg navbar-light bg-light">
  <div class="collapse navbar-collapse" id="navbarSupportedContent">
    <ul class="navbar-nav mr-auto">
      <li class="nav-item <%= 'active' if controller_name=='home' %>">
        <%= link_to  t('home'), root_path, class: 'nav-link' %>
      </li>
      <% if current_employee %>
      <% end %>
    </ul>
    <form class="form-inline my-2 my-lg-0">
      <% if current_employee %>
        <ul class="navbar-nav mr-auto my-2 my-lg-0">
          <li class="nav-item dropdown">
            <a class="nav-link dropdown-toggle" href="#" id="navbarDropdownUser" role="button" data-toggle="dropdown" aria-haspopup="true" aria-expanded="false">
              <%= current_employee.name %>
            </a>
            <div class="dropdown-menu" aria-labelledby="navbarDropdownUser">
              <%= link_to  t('profile'), employees_show_path, class: 'dropdown-item' %>
              <%= link_to  t('change_password'), edit_employee_registration_path, class: 'dropdown-item' %>              
              <div class="dropdown-divider"></div>
              <%= link_to t('signout'), destroy_employee_session_path, method: :delete, class: 'dropdown-item' %>
            </div>
          </li>
        </ul>
      <% else %>
        <%= link_to t('signin'), new_employee_session_path, class: 'btn btn-large btn-primary' %>
      <% end %>
    </form>
  </div>
</nav>
